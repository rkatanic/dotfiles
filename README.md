# 🚀 Installation

Run the setup wizard to install dependencies and link configurations:

```
chmod +x setup.sh
./setup.sh
```

## 👤 Git Setup
After linking, set your local Git identity:
```
git config --global user.name "Firstname Lastname"
git config --global user.email "your.email@example.com"
```
